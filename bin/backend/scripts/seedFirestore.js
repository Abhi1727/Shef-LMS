#!/usr/bin/env node

// Archived Firestore seeding script moved from backend/scripts/seedFirestore.js

console.log('seedFirestore.js is archived in bin/. Firestore seeding has been removed.');
console.log('Use MongoDB seeding scripts (e.g., backend/scripts/seedData.js) instead.');
