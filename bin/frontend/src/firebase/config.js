// Archived Firebase frontend config moved from frontend/src/firebase/config.js
// Kept only so older import paths can be recovered if needed.

export const auth = null;
export const db = null;
export const storage = null;

const app = null;
export default app;
