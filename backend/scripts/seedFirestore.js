#!/usr/bin/env node

// Deprecated Firestore seeding script.
// Firestore is no longer used by the backend; all data
// should now be seeded into MongoDB using Mongoose-based
// scripts such as scripts/seedData.js.

console.log('⚠️ seedFirestore.js is deprecated. Firestore seeding has been removed.');
console.log('   Use MongoDB seeding scripts (e.g., scripts/seedData.js) instead.');
