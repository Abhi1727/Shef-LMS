// NOTE: Firebase Admin has been fully removed from the backend.
// This file is kept only as a legacy stub so that any old
// imports like `require('./config/firebase')` do not crash.
//
// Do NOT add new code that depends on Firebase here.

const admin = null;
const db = null;
const storage = null;

module.exports = { admin, db, storage };
